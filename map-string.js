const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase()); // toUpperCase = deixar tudo em maiúsculo

console.log(nomesPadronizados)
