const notas = [7, 7, 8, 9];
const novaListaNotas = [...notas, 10]; // spread operator = para clonar arrays (se usa: ...) // 10 = para adicionar o que queremos
                                      // como se fosse: novaListaNotas.push(10);

console.log(notas);
console.log(novaListaNotas);
